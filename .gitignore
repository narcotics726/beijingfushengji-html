# ── 系统 / 编辑器 ───────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.log

# ── 依赖 / 工具链 ───────────────────────────────────────────
node_modules/
.npm-cache/

# ── 原始游戏源码：仅作本地开发参考 ────────────────────────────
# reference/ 为从 GitHub 克隆的 C++/MFC 原版源码（约 23MB，上游已托管）。
# 默认不纳入本仓库；若希望跟踪，删除下一行并重新 git add reference
reference/

# ── 编译 / 二进制 ───────────────────────────────────────────
*.exe
*.obj
*.pdb
*.dll
*.lib

# ── 构建产物 ────────────────────────────────────────────────
dist/

# ── GBK 转码临时目录（读 reference 源码用）───────────────────
_u8tmp/

# ── 全量素材重建输出（`npm run assets -- --all`，不参与构建）──
_assets_all/

# ── Obsidian / 临时抓取 ─────────────────────────────────────
.scratch_*/
.tmp/

# ── 可选：转档生成产物（若用脚本构建且不想入库，取消注释）─────
# assets/img/
# assets/audio/
